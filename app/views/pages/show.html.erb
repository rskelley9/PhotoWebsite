<% content_for :title, @page.name %>
<%= image_tag(@page.image.url, :class => 'page-image') if @page.image_uid %>
<%= @page.body.to_html.html_safe %>

<% if @page.keywords.split(',').include?("pricing") %>

<%= render partial: 'admin/shared/slider-detail' %>

<!-- StartPricing -->
<%= render partial: 'admin/shared/pricing-table' %>

<% end %>

<style>
  .page-image{
margin: 0px 20px 20px 20px;
float: right;
height: 240px;
width: 300px;
  }

  p{
    font-family:Varela-Round, sans-serif;
    font-size: 16px;
  }

h1{
/*  font-family: Varela-Round, sans-serif;*/
}

</style>